# T04 · 오늘의 진짜 정보판 — 요구사항 정리

출처: Notion `T04 · 오늘의 진짜 정보판` (https://app.notion.com/p/T04-3c10741730ea81ee9a27f04df5cd62e6)

## 한 줄 정의
날씨·환율·버스·게임 서버 상태 등 **실제로 변하는 데이터 한 가지**를,
**값·출처·조회 시각**과 함께 보여주는 **개인 정보판(공개 웹페이지, 설치 없이 접속)**.

- 난이도 계획시간: 6~7h (능동 작업시간만. 대기 시간 제외)
- 별도 경과 기간 필요: **서로 다른 날짜의 실제 기록 2건** (하루 이상 간격을 두고 자연 발생해야 함 — 몰아서 만들 수 없음)

## 완료 기준 (전체)
1. 공개 주소가 설치 없이 열리고, 현재값·단위·출처·조회 시각이 한 화면에 보인다.
2. 출처 주소를 한 번 누르면 원자료 페이지가 열린다.
3. 현재 자료 / 오래된 자료의 상태와, 이전 값과의 차이가 화면에서 구분된다.

## 카드별 요구사항

### 카드 1 — 값의 맥락
- **목표**: 변하는 데이터 하나를 가져와 값·단위·출처·마지막 조회 시각을 한 화면에 표시.
- **통과 기준**: 현재값·단위·출처·조회 시각이 한 화면에 보인다.
- **먼저 할 일**:
  - "이 정보판은 ______를 확인하기 위한 것이다" 문장을 채운다 → **어떤 데이터를 다룰지 결정 필요**.
  - 공개 문서·이용 조건이 있고 단위/갱신 시각이 분명한 출처를 고른다.

### 카드 2 — 비밀키와 호출 경로
- **목표**: 원하는 호출 방법을 선택하되, 브라우저 / 배포 파일 / 네트워크 주소 / Git 기록에 **비밀값을 0건** 남긴다.
- **통과 기준**: 출처 주소가 화면에 보이고, 누르면 원자료 페이지가 열린다.
- 특정 API·호출 경로 강제 없음 — 단, 키가 필요한 API를 쓰려면 **서버리스 프록시** 등으로 키를 숨겨야 함. 가능하면 키 없이 쓸 수 있는 공개 API를 우선 검토.

### 카드 3 — 실패를 구분해 보여주기
- **목표**: timeout · 인증 실패 · 호출 제한(rate limit) · 오프라인 · 응답 형식 변경, 5종 장애를 각각 재현하고 서로 다른 상태로 표시.
- **통과 기준**: 현재 자료 / 오래된 자료가 상태 문구로 구분된다.
- 마지막 정상값 유지 + "오래된 데이터" 표시 + 재시도 동작 필요. 정상값이 없을 때 값이 있는 것처럼 꾸미면 안 됨.

### 카드 4 — 하루 한 번 기록
- **목표**: 기준 시간대(타임존) 명시, 날짜별 값 1개만 저장. 같은 날 중복 기록 방지.
- **통과 기준**: 날짜별 값이 같은 날짜 중복 없이 목록에 보인다.
- 저장 식별자 = `날짜 + 데이터 종류`. 첫날 저장 후 실제 다음 날짜까지 기다려야 둘째 기록이 생김 (자연 경과 필요).

### 카드 5 — 어제와 비교 검증
- **목표**: 두 날짜의 원자료·저장값·변화 계산값·화면값을 대조하고, 이전 기록 대비 변화(차이/방향/단위)를 표시.
- **통과 기준**: 이전 값과 현재 값의 차이·방향·단위가 한 화면에 보인다.
- 단위/기준 시각 불일치, 자료 부족 시 비교값을 억지로 표시하지 않는다.

## 제출물 (전체 공통)
1. **검증 안내서** 1개 — `어디로 가나요 / 무엇을 하나요(3단계 이내, 30초 내) / 무엇이 보이면 통과인가요 / 안 될 때`
2. **AI 3줄** — `AI에게 맡긴 일` / `내가 판단한 일` / `AI 말을 안 들은 일`
3. 제출 화면에는 이 두 가지만 남긴다 (다른 산출물은 화면에 안 남겨도 됨, 근거 자료로만 보관).

## 강사 판정
5개 카드를 순서대로 읽게 돕고, 통과 판단은 기존 제출·강사 확인·오너 최종 인정 계약을 따름 → 오너가 공개 주소를 **15초 안**에 판정.

## 핵심 제약 요약
- 비밀값(토큰/키/개인정보) 0건: 브라우저 코드, 공개 파일, 네트워크 요청, Git 기록, 제출 기록 전부.
- 정상값이 없을 때 가짜/이전 값을 정상인 척 보여주지 않는다.
- 같은 날 여러 번 실행해도 기록은 1건만 남는다 (dedupe by date+종류).
- 기다리는 시간(경과 기간)과 능동 작업시간(6~7h)을 분리해서 기록한다.
- 개인정보를 화면/파일/제출 기록에 남기지 않는다.
